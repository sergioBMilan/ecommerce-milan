# Skills

Cada skill custom va en su propia subcarpeta:

```
.claude/skills/
└── mi-skill/
    └── SKILL.md
```

El `SKILL.md` debe tener un frontmatter con `name` y `description` para que Claude Code lo descubra.
